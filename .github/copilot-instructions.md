# Copilot Instructions

- Always run `ruff check . --fix` after making code changes.
- If Ruff reports remaining issues, resolve them before finishing.
- Run `source .venv/bin/activate` to activate the virtual environment.
- Python code is launched with `python -m src.bazosbot.main`